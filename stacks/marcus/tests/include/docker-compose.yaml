services:
  tests:
    build:
      dockerfile: include/bats.Dockerfile
      context: /tmp/.nilcontext
    volumes:
      - $TESTS_DIR:/tests
      - $TESTS_DIR/helpers:/helpers
    command:
      - -p
      - .
